{
  "name": "remote-request-client",
  "version": "0.0.1",
  "description": "A client capable of communicating with a remote request server",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev src/index.ts --respawn --transpile-only",
    "build": "tsc",
    "push": "git push origin main",
    "example": "npm run build && node examples/sample.js",
    "sandbox": "npm run build && node examples/test.js"
  },
  "keywords": [],
  "author": "Miguel Silva Pinheiro <LearXD>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/uuid": "^9.0.8",
    "@types/ws": "^8.5.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "uuid": "^9.0.1",
    "ws": "^8.16.0"
  }
}